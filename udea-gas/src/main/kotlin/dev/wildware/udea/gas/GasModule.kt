package dev.wildware.udea.gas

/**
 * Abilities, attributes and effects, tick-denominated throughout (spec 5: Tick is
 * universal). Context parameters are confined to this module's authoring surface and are
 * opted into here, never in udea.kotlin-library (spec 7).
 *
 * This object is a placeholder so the module has a source root and appears in an IDE
 * sync. Later Phase 0 waves replace it with the real declarations.
 */
internal object GasModule
