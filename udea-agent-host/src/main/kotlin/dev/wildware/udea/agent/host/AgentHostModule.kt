package dev.wildware.udea.agent.host

/**
 * The HTTP host for the agent surface. Debug-only and verified absent from release, so
 * nothing on a shipping runtime classpath may depend on it -- note that moba does not.
 *
 * This object is a placeholder so the module has a source root and appears in an IDE
 * sync. Later Phase 0 waves replace it with the real declarations.
 */
internal object AgentHostModule
