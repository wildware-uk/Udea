package dev.wildware.udea.assets

/**
 * The runtime asset model and the .udeapak reader. Only pack-time-stable AssetIndex ints
 * ever reach a snapshot, so this module is not on the snapshot path (spec 3.6).
 *
 * This object is a placeholder so the module has a source root and appears in an IDE
 * sync. Later Phase 0 waves replace it with the real declarations.
 */
internal object AssetsModule
