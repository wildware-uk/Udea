// The field `MobaEntry.seed` fills. Declared rather than inferred so that the packer's
// reachability walk has a root to start from - see `BundleContent.reachable`.
level(name = "arena", entities = listOf(reference("blueprint/grunt")))
