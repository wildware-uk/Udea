// The game's screen effects. A `.frag` is an asset like a model: declared here, read and checked by
// the build, packed with the text in it, and named in Kotlin as `GameAssets.shaders.scanlines`.
shader(name = "scanlines", file = "shaders/scanlines.frag")
