package dev.wildware.udea.net

/**
 * Transports, baselines, relevancy, prediction and RPC. The snapshot ring in udea-core is
 * the replication baseline store, so this module reads it rather than keeping its own
 * (spec 3.1).
 *
 * This object is a placeholder so the module has a source root and appears in an IDE
 * sync. Later Phase 0 waves replace it with the real declarations.
 */
internal object NetModule
