<?xml version="1.0" encoding="utf-8"?>
<!--
  The phone build's one activity. No permissions: the simulation reads a packed bundle out of this
  APK's own resources and talks to nothing, and a client that joins a server over the network is
  the ticket that adds INTERNET along with the transport that needs it.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="moba"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name="dev.wildware.moba.android.MobaActivity"
            android:exported="true"
            android:label="moba">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
