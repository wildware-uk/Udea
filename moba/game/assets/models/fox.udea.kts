// The Khronos Fox (issue #240), in the game's own asset root so the build generates its typed
// clips - `Fox.Clips.Survey`, `.Walk` and `.Run` - for game code to play (issue #241). The file is
// the same bytes as `example-assets/models/fox/Fox.glb`; `fox/NOTICE.md` is its licence (CC BY 4.0).

model(name = "fox", file = "models/fox/Fox.glb")
