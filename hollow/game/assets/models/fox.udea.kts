// The Khronos Fox (issue #251): Hollow's creature, in the game's own asset root so the build
// generates its typed clips - `Fox.Clips.Survey`, `.Walk` and `.Run` - for the fox's state machine
// to play. The same bytes as moba's copy; `fox/NOTICE.md` is its licence (CC BY 4.0) and names the
// test that keeps the two copies identical.

model(name = "fox", file = "models/fox/Fox.glb")
