// Quaternius' Animated Human (issue #244), an FBX: the build converts it to glTF, so it is the same
// kind of model as the fox, with typed clips generated from it - `Human.Clips.Idle`, `.Walk`, `.Run`
// and `.Punch`. `human/NOTICE.md` is its licence (CC0) and says what was changed.

model(name = "human", file = "models/human/Human.fbx")
