// The game's models. This file is in `assets/models/`, so what it declares is generated as
// `GameAssets.models.<name>`: the folder a declaration's script sits in is the accessor's group.
// A script at the top of `assets/` declares into `GameAssets.root` instead.
//
// `rover.glb` is written by `make_rover.py`, beside it. Replace it with your own model and keep
// the name, and nothing in the Kotlin has to change.

model(name = "rover", file = "models/rover.glb")
