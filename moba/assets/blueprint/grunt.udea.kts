// The one blueprint `moba` spawns. `MobaModule` holds the runtime component list; this is the
// authored side of the same unit, and the id is what `level/arena` names.
blueprint(name = "grunt", components = listOf("dev.wildware.moba.Position"))
