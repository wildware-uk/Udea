package dev.wildware.moba

/**
 * The example game. Depends on udea-render with implementation, not api, so GL stays
 * inside udea-render. udea-agent-host is deliberately absent: it is debug-only and must
 * be verifiable as absent from a release build.
 *
 * This object is a placeholder so the module has a source root and appears in an IDE
 * sync. Later Phase 0 waves replace it with the real declarations.
 */
internal object MobaModule
