// A `.frag` declared as an asset, so this corpus exercises the one asset kind whose *contents*
// travel in the packed graph rather than as a path to a file a platform opens.
shader(name = "tint", file = "shaders/tint.frag")
